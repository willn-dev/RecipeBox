npx tailwindcss -i ./static/styles/app.css -o ./static/styles/output.css
for generating a single output
to monitor continually -> add flag --watch

DAISY uses the tailwind concept of positioning via the html 
tailwind classes for this would be justify-center, not justify-content: center;


OK so return of the join_table query index in this order:

recipe_id, ingredient_id, qty, | ingredient_id, name
0           1              2        3            4
DONE!

FOR RECREATING DB:
pg_dump --schema-only -d recipebowl > schema.sql


TODOS:

Change delete icon maybe change by theme if I do it as a button with an icon on it maybe?
Need a home button in top left of all pages but index
